package com.example.app_rpg.ui.theme

import androidx.compose.ui.graphics.Color

val corMestrePrincipal = Color(0xFF7C2AB8)

val corMestreDestaque = Color(0xFFA35FD3)

val corMestreSombra = Color(0xFF2A1640)

// Ações primárias, botões (Salvar/Cancelar) e controles
val corJogadorPrincipal = Color(0xFFF0703B)

// Alertas, rolagem de dados e encontros
val corJogadorDestaque = Color(0xFFFF9D5E)

// Fundo de painéis e áreas secundárias
val corJogadorEscuro = Color(0xFF4A1A05)

// Fundo geral do aplicativo (tela principal)
val corPretoPuro = Color(0xFF000000)

// Bordas estruturais, modais, fundo dos inputs/cards inativos
val corCinzaEscuro = Color(0xFF1E1E24)

// Textos secundários, labels de atributos (For, Des, HP)
val corCinzaMedio = Color(0xFF8B8B99)

// Fundo de áreas de destaque, perícias ou cartas
val corCinzaClaro = Color(0xFFEBEBEB)

// Texto de leitura longa (menos fadiga visual)
val corBrancoOffWhite = Color(0xFFF2F2F5)

// Títulos em destaque, valores numéricos principais (ex: 56, 23)
val corBrancoPuro = Color(0xFFFFFFFF)


